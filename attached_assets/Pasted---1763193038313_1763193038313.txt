حل مشكلة الرموز الغريبة في النصوص العربية

لاحظت من الصورة أن هناك مشكلة في عرض الأحرف العربية حيث تظهر رموز غريبة (pPbPbOpBpbF etc). هذه المشكلة شائعة وتحدث بسبب:

🔍 أسباب المشكلة الرئيسية

1. مشاكل ترميز الأحرف (Encoding)

· عدم استخدام UTF-8 بشكل صحيح
· تكوين خاطئ للغة في إعدادات المشروع

2. مشاكل في الخطوط (Fonts)

· عدم دعم الخطوط المستخدمة للغة العربية
· استيراد خاطئ للخطوط

🛠️ الحلول الفورية - خطوة بخطوة

الخطوة 1: إصلاح ترميز UTF-8 في الملفات الرئيسية

```html
<!-- في public/index.html -->
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <title>مركز AZ المالي</title>
</head>
```

الخطوة 2: إضافة إعدادات اللغة في Vite Config

```javascript
// vite.config.js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  build: {
    outDir: 'dist',
  },
  server: {
    headers: {
      'Content-Type': 'text/html; charset=utf-8',
    }
  }
})
```

الخطوة 3: إضافة خطوط عربية مدعومة

```css
/* في ملف CSS الرئيسي */
@import url('https://fonts.googleapis.com/css2?family=Noto+Naskh+Arabic:wght@400;500;600;700&display=swap');

* {
  font-family: 'Noto Naskh Arabic', 'Segoe UI', Tahoma, sans-serif;
}

body {
  direction: rtl;
  text-align: right;
  font-family: 'Noto Naskh Arabic', 'Segoe UI', Tahoma, sans-serif;
}
```

الخطوة 4: إصلاح إعدادات اللغة في package.json

```json
{
  "name": "az-finance-hub",
  "version": "1.0.0",
  "type": "module",
  "description": "تطبيق إدارة مالية باللغة العربية",
  "keywords": ["finance", "arabic", "react"],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

🔧 الحلول التقنية الإضافية

إذا استمرت المشكلة، جرب هذا الحل في main.jsx:

```jsx
// main.jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'

// إضافة لضبط ترميز اللغة
document.addEventListener('DOMContentLoaded', function() {
  document.documentElement.setAttribute('lang', 'ar');
  document.documentElement.setAttribute('dir', 'rtl');
});

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

إنشاء ملف تحكم في اللغة:

```javascript
// utils/languageConfig.js
export const setupArabicLanguage = () => {
  // ضبط ترميز الصفحة
  if (typeof document !== 'undefined') {
    const metaCharset = document.querySelector('meta[charset]');
    if (metaCharset) {
      metaCharset.setAttribute('charset', 'UTF-8');
    }
    
    document.documentElement.lang = 'ar';
    document.documentElement.dir = 'rtl';
  }
};

export const arabicFonts = {
  primary: "'Noto Naskh Arabic', 'Segoe UI', Tahoma, sans-serif",
  fallback: "Arial, sans-serif"
};
```

📝 خطوات التنفيذ السريعة لـ Agent 3

المهام المطلوبة (بتكلفة منخفضة):

1. تحديث index.html بإضافة charset="UTF-8"
2. إضافة استيراد خطوط Google Arabic في CSS
3. تحديد dir="rtl" في العنصر HTML
4. اختبار العرض بعد كل تغيير

الكود الأساسي المطلوب فقط:

```html
<!-- الخطوة الوحيدة الأكثر أهمية -->
<meta charset="UTF-8">
```

```css
/* الخطوة الثانية */
@import url('https://fonts.googleapis.com/css2?family=Noto+Naskh+Arabic&display=swap');
body { font-family: 'Noto Naskh Arabic', sans-serif; }
```

🎯 النتيجة المتوقعة

بعد تطبيق هذه الحلول، سيتم:

· ✅ عرض النصوص العربية بشكل صحيح
· ✅ دعم اتجاه النص من اليمين لليسار
· ✅ تحسين تجربة المستخدم العربي
· ✅ حل مشكلة الرموز الغريبة